#include <bits/stdc++.h>
using namespace std;

int a[10] = {-1}, n = 10;
int main()
{
	cout << "Enter the priority and the element!" << endl;
	for (int i = 0; i < n; i++)
	{
		int pri, ele;
		cin >> pri >> ele;
		// if()
		// a[pri]=ele;
	}

	return 0;
}